


#commandes pour lire une entrée
$1/read -p "Enter: " name
name=${name:-Richard}

#création d'un dossier du même nom
mkdir $name
cd $name

#Crée l'environnement pour developper
swift package init --type=executable

#On se déplace dans le répertoire du fichier principal
cd Sources/$name/

#Crée un fichier qui pourra exécuter le fichier pour le pc
touch run
echo "


swift build
cd ..
cd ..
./.build/debug/$name
" > run

chmod +x run

#remplacer le fichier créé par un fichier vide du même nom
rm main.swift

#Aller dans notre code principal
vim main.swift


